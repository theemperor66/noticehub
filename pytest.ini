[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --cov=src --cov-report=html --cov-report=term-missing -vv
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:pydantic._internal._config:
